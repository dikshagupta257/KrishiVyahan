<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codingblocksmodules.agrome">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgroMe"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.view.activities.ReUpyogItemActivity" />
        <activity
            android:name=".ui.view.activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.view.activities.CreatePostActivity" />
        <activity android:name=".ui.view.activities.ShopActivity" />
        <activity android:name=".ui.view.activities.EditProfileActivity" />
        <activity android:name=".ui.view.activities.ProfileActivity" />
        <activity android:name=".ui.view.activities.ConsumerActivity" />
        <activity
            android:name=".ui.view.activities.SignInActivity"
            android:theme="@style/Theme.AgroMe.NoActionBar" />
        <activity
            android:name=".ui.view.activities.SignUpActivity"
            android:theme="@style/Theme.AgroMe.NoActionBar" />
        <activity android:name=".ui.view.activities.SellerActivity" />
    </application>

</manifest>